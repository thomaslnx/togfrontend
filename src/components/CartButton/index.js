import { FaShoppingBag } from 'react-icons/fa';
import { Button } from './styles';

const CartButton = () => {
  return (
    <Button>
      <FaShoppingBag />
    </Button>
  );
};

export default CartButton;
